// import { redirect } from "next/navigation";

const Redirection2 = () => {
  // when we are trying to access the localhost:3000/redirect2, it will redirected to localhost:3000/ route
  // redirect("/");

  return <div>Redirection Page 2</div>;
};

export default Redirection2;
