// import { redirect } from "next/navigation";

const Redirection = () => {
  // when we are trying to access the localhost:3000/redirect, it will redirected to localhost:3000/ route
  // redirect("/");

  return <div>Redirection Page 1</div>;
};

export default Redirection;
